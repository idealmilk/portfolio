import React from "react";

import { Gallery, Ticker } from "components";

const WorkContainer = () => {
  return (
    <section id="work">
      <Ticker text="Frontend Virtuosity" />
      <Gallery />
    </section>
  );
};

export default WorkContainer;
